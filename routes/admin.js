const express = require("express");

const router = express.Router();

const adminController = require("../controllers/admin");
// router.post("/addProduct", adminController.addProduct);
// router.get("/adminProducts", adminController.adminProducts);
// router.delete("/productDelete/:productId", adminController.productDelete);
// router.get("/productEdit/:productId", adminController.productEdit);
// router.post("/updateProduct", adminController.updateProduct);
module.exports = router;
